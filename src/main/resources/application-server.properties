spring.thymeleaf.check-template-location=true
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
spring.thymeleaf.mode=HTML5
spring.thymeleaf.encoding=UTF-8
spring.thymeleaf.content-type=text/html
spring.thymeleaf.cache=false


# MSSQL connection properties
spring.datasource.url=jdbc:sqlserver://10.156.5.241;databaseName=HWY_MAILLOG
spring.datasource.username=hwy_maillogdbo
spring.datasource.password=YKpass0721@@
spring.datasource.driverClassName=com.microsoft.sqlserver.jdbc.SQLServerDriver

# Log JPA queries
# Comment this in production
spring.jpa.show-sql=false

# Drop and create new tables (create, create-drop, validate, update)
# Only for testing purpose - comment this in production
spring.jpa.hibernate.ddl-auto=none

# Hibernate SQL dialect
spring.jpa.hibernate.dialect=org.hibernate.dialect.SQLServer2012Dialect

# Set the file size limit (default 1Mb). If you want to specify that files be
# unlimited set the multipart.maxFileSize property to -1.
multipart.maxFileSize = 3Mb

# Set the total request size for a multipart/form-data (default 10Mb)
multipart.maxRequestSize = 20Mb


#H2 DataBase----
#spring.datasource.url= jdbc:h2:tcp://localhost/~/test
#spring.datasource.driverClassName=org.h2.Driver
#spring.datasource.username=sa
#spring.datasource.password=1234
#spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
#spring.jps.show-sql=true
#spring.jpa.Hibernate.hbm2ddl=none;